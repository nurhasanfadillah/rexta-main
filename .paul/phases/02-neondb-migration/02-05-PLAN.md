---
phase: 02-neondb-migration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: ["services/database.ts", "App.tsx", "services/supabaseClient.ts", "package.json"]
autonomous: true
---

<objective>
## Goal
Ganti semua Supabase SDK calls di frontend dengan `fetch()` ke API endpoints baru, replace auth dengan Better Auth client, hapus `@supabase/supabase-js` dari dependencies, dan bersihkan dead code.

## Purpose
Ini adalah plan terakhir di Phase 02 — menghubungkan frontend yang ada ke backend API layer yang sudah dibangun di Plan 02-01 hingga 02-04. Setelah ini, app tidak bergantung lagi pada Supabase SDK dan bisa deploy ke Vercel dengan NeonDB + Better Auth.

## Output
- `services/database.ts` — rewrite lengkap: Supabase → fetch() + Better Auth client
- `App.tsx` — hapus import supabase + hapus onAuthStateChange block
- `services/supabaseClient.ts` — replaced with stub (no longer imported)
- `package.json` — remove `@supabase/supabase-js`
</objective>

<context>
## Prior Work
@.paul/phases/02-neondb-migration/02-02-SUMMARY.md
@.paul/phases/02-neondb-migration/02-03-SUMMARY.md
@.paul/phases/02-neondb-migration/02-04-SUMMARY.md

## Source Files
@services/database.ts
@services/supabaseClient.ts
@App.tsx
@package.json
@types.ts
</context>

<acceptance_criteria>

## AC-1: Auth Flow Berfungsi via Better Auth
```gherkin
Given user mengakses app
When app mount, getCurrentSession() dipanggil
Then session dari Better Auth cookie dikembalikan (atau null jika tidak ada)

Given user submit form login
When signIn(email, password) dipanggil
Then Better Auth /api/auth/sign-in/email dipanggil dan session cookie di-set

Given user klik logout
When signOut() dipanggil
Then Better Auth /api/auth/sign-out dipanggil dan session dihapus
```

## AC-2: Data Fetching via fetch()
```gherkin
Given user terautentikasi
When fetchAllData() dipanggil
Then categories di-fetch dari /api/categories (public, no auth needed)
     return shape { products: [], materials: [], categories: [], transactions: [] } terjaga
```

## AC-3: CRUD Operations via fetch()
```gherkin
Given user terautentikasi
When apiAddProduct(product) / apiUpdateProduct(product) / apiDeleteProduct(id) dipanggil
Then fetch() dikirim ke /api/products dengan method dan body yang benar
     return shape { error: null } jika sukses, { error: Error } jika gagal
     (shape compatible dengan App.tsx yang melakukan const { error } = await apiFn())
```

## AC-4: Transaction + Dashboard via fetch()
```gherkin
Given user terautentikasi
When apiAddTransactionAndUpdateStock(tx, itemId, itemType, manualStock) dipanggil
Then POST /api/transactions dipanggil dengan body snake_case
     return { newStock: number, error: null } jika sukses

When getDashboardSummary() dipanggil
Then GET /api/dashboard dipanggil
     return shape compatible dengan DashboardView (totalAssetValue, productAssets, materialAssets, productCount, materialCount, lowStockCount)
```

## AC-5: Supabase SDK Terhapus dari Codebase
```gherkin
Given migration complete
When npm run build dijalankan
Then tidak ada import dari @supabase/supabase-js tersisa di source files
     App.tsx tidak punya import { supabase } lagi
     onAuthStateChange subscription dihapus dari App.tsx
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite services/database.ts</name>
  <files>services/database.ts</files>
  <action>
    Rewrite lengkap `services/database.ts`. Hapus semua import Supabase. Ikuti struktur ini:

    **1. Imports + Auth Client:**
    ```typescript
    import { createAuthClient } from 'better-auth/client';
    import { InventoryData, Product, Material, Category, Transaction, ItemType } from '../types';

    const authClient = createAuthClient();
    ```

    **2. Fetch helper (internal, tidak di-export):**
    ```typescript
    const apiFetch = async (url: string, options?: RequestInit) => {
      const res = await fetch(url, {
        ...options,
        credentials: 'include',
        headers: { 'Content-Type': 'application/json', ...options?.headers },
      });
      if (!res.ok) {
        const body = await res.json().catch(() => ({}));
        throw Object.assign(new Error(body.error || 'Request failed'), { status: res.status });
      }
      return res.json();
    };
    ```

    **3. logError helper (jaga seperti sebelumnya):**
    ```typescript
    const logError = (context: string, error: any) => {
      console.error(`[DB Error] ${context}:`, error?.message || JSON.stringify(error));
      return { error };
    };
    ```

    **4. AUTH FUNCTIONS:**
    ```typescript
    export const signIn = async (email: string, password: string) => {
      return await authClient.signIn.email({ email, password });
    };
    export const signOut = async () => {
      return await authClient.signOut();
    };
    export const getCurrentSession = async () => {
      const { data } = await authClient.getSession();
      return data?.session || null;
    };
    ```

    **5. fetchAllData** → GET /api/categories (public, jaga return shape):
    ```typescript
    export const fetchAllData = async (): Promise<InventoryData> => {
      try {
        const categories = await apiFetch('/api/categories');
        return { products: [], materials: [], categories: categories || [], transactions: [] };
      } catch (error) {
        logError('fetchAllData', error);
        return { products: [], materials: [], categories: [], transactions: [] };
      }
    };
    ```

    **6. getPublicProductsPaginated** → GET /api/products/public:
    ```typescript
    export const getPublicProductsPaginated = async (page: number, limitCount: number, search = '') => {
      try {
        const params = new URLSearchParams({ page: String(page), limit: String(limitCount) });
        if (search) params.set('search', search);
        const result = await apiFetch(`/api/products/public?${params}`);
        return { data: result.data || [], categories: result.categories || [], error: null, count: result.count || 0 };
      } catch (error) {
        logError('getPublicProductsPaginated', error);
        return { data: [], categories: [], error, count: 0 };
      }
    };
    ```

    **7. getDashboardSummary** → GET /api/dashboard (remap field names untuk DashboardView):
    ```typescript
    export const getDashboardSummary = async () => {
      try {
        const result = await apiFetch('/api/dashboard');
        return {
          totalAssetValue: result.totalAssetValue,
          productAssets: result.productAssetValue,
          materialAssets: result.materialAssetValue,
          productCount: result.totalProducts,
          materialCount: result.totalMaterials,
          lowStockCount: 0, // tidak tersedia di API baru
        };
      } catch (error) {
        logError('getDashboardSummary', error);
        return null;
      }
    };
    ```

    **8. getFavoriteProducts** → GET /api/products/favorites (returns array):
    ```typescript
    export const getFavoriteProducts = async () => {
      try {
        const data = await apiFetch('/api/products/favorites');
        return data || [];
      } catch (error) {
        logError('getFavoriteProducts', error);
        return [];
      }
    };
    ```

    **9. getRecentTransactionsPaginated** → GET /api/transactions:
    ```typescript
    export const getRecentTransactionsPaginated = async (page: number, limitCount: number) => {
      try {
        const params = new URLSearchParams({ page: String(page), limit: String(limitCount) });
        const result = await apiFetch(`/api/transactions?${params}`);
        return { data: result.data || [], error: null, count: result.count || 0 };
      } catch (error) {
        logError('getRecentTransactionsPaginated', error);
        return { data: [], error, count: 0 };
      }
    };
    ```

    **10. getTransactionHistory** → GET /api/transactions?itemId=:
    ```typescript
    export const getTransactionHistory = async (itemId: string, page = 1, limitCount = 10) => {
      if (!itemId) return { data: [], count: 0 };
      try {
        const params = new URLSearchParams({ itemId, page: String(page), limit: String(limitCount) });
        const result = await apiFetch(`/api/transactions?${params}`);
        return { data: result.data || [], count: result.count || 0 };
      } catch (error) {
        logError('getTransactionHistory', error);
        return { data: [], count: 0 };
      }
    };
    ```

    **11. getTransactionsByDateRange** → GET /api/transactions?dateFrom=&dateTo= (jaga date conversion logic):
    ```typescript
    export const getTransactionsByDateRange = async (startDate: string, endDate: string) => {
      try {
        if (!startDate || !endDate) return [];
        const startParts = startDate.split('-').map(Number);
        const endParts = endDate.split('-').map(Number);
        const startObj = new Date(startParts[0], startParts[1] - 1, startParts[2], 0, 0, 0, 0);
        const endObj = new Date(endParts[0], endParts[1] - 1, endParts[2], 23, 59, 59, 999);
        const params = new URLSearchParams({
          dateFrom: startObj.toISOString(),
          dateTo: endObj.toISOString(),
          limit: '1000',
        });
        const result = await apiFetch(`/api/transactions?${params}`);
        return result.data || [];
      } catch (error) {
        logError('getTransactionsByDateRange', error);
        return [];
      }
    };
    ```

    **12. getProductsPaginated** → GET /api/products:
    ```typescript
    export const getProductsPaginated = async (page: number, limitCount: number, search = '', categoryId = '', onlyFavorites = false) => {
      try {
        const params = new URLSearchParams({ page: String(page), limit: String(limitCount) });
        if (search) params.set('search', search);
        if (categoryId && categoryId !== 'SEMUA') params.set('categoryId', categoryId);
        if (onlyFavorites) params.set('onlyFavorites', 'true');
        const result = await apiFetch(`/api/products?${params}`);
        return { data: result.data || [], error: null, count: result.count || 0 };
      } catch (error) {
        logError('getProductsPaginated', error);
        return { data: [], error, count: 0 };
      }
    };
    ```

    **13. getMaterialsPaginated** → GET /api/materials:
    ```typescript
    export const getMaterialsPaginated = async (page: number, limitCount: number, search = '') => {
      try {
        const params = new URLSearchParams({ page: String(page), limit: String(limitCount) });
        if (search) params.set('search', search);
        const result = await apiFetch(`/api/materials?${params}`);
        return { data: result.data || [], error: null, count: result.count || 0 };
      } catch (error) {
        logError('getMaterialsPaginated', error);
        return { data: [], error, count: 0 };
      }
    };
    ```

    **14. Products CRUD** (semua return { data, error } compatible):
    ```typescript
    export const apiAddProduct = async (product: Product) => {
      try {
        const data = await apiFetch('/api/products', { method: 'POST', body: JSON.stringify({ id: product.id, name: product.name, category_id: product.categoryId, price_cmt: product.priceCMT, hpp: product.hpp, stock: product.stock, is_favorite: product.isFavorite || false }) });
        return { data: [data], error: null };
      } catch (error) { return { data: null, error }; }
    };

    export const apiUpdateProduct = async (product: Product) => {
      try {
        const data = await apiFetch(`/api/products?id=${product.id}`, { method: 'PUT', body: JSON.stringify({ name: product.name, category_id: product.categoryId, price_cmt: product.priceCMT, hpp: product.hpp, stock: product.stock, is_favorite: product.isFavorite || false }) });
        return { data: [data], error: null };
      } catch (error) { return { data: null, error }; }
    };

    export const apiToggleProductFavorite = async (id: string, _isFavorite: boolean) => {
      try {
        await apiFetch(`/api/products/${id}/favorite`, { method: 'PATCH' });
        return { error: null };
      } catch (error) { return { error }; }
    };

    export const apiDeleteProduct = async (id: string) => {
      try {
        await apiFetch(`/api/products?id=${id}`, { method: 'DELETE' });
        return { error: null };
      } catch (error) { return { error }; }
    };
    ```

    **15. Materials CRUD:**
    ```typescript
    export const apiAddMaterial = async (material: Material) => {
      try {
        const data = await apiFetch('/api/materials', { method: 'POST', body: JSON.stringify({ id: material.id, name: material.name, unit: material.unit, price: material.price, stock: material.stock }) });
        return { data: [data], error: null };
      } catch (error) { return { data: null, error }; }
    };

    export const apiUpdateMaterial = async (material: Material) => {
      try {
        const data = await apiFetch(`/api/materials?id=${material.id}`, { method: 'PUT', body: JSON.stringify({ name: material.name, unit: material.unit, price: material.price, stock: material.stock }) });
        return { data: [data], error: null };
      } catch (error) { return { data: null, error }; }
    };

    export const apiDeleteMaterial = async (id: string) => {
      try {
        await apiFetch(`/api/materials?id=${id}`, { method: 'DELETE' });
        return { error: null };
      } catch (error) { return { error }; }
    };
    ```

    **16. Categories CRUD:**
    ```typescript
    export const apiAddCategory = async (category: Category) => {
      try {
        const data = await apiFetch('/api/categories', { method: 'POST', body: JSON.stringify({ id: category.id, name: category.name }) });
        return { data: [data], error: null };
      } catch (error) { return { data: null, error }; }
    };

    export const apiDeleteCategory = async (id: string) => {
      try {
        await apiFetch(`/api/categories?id=${id}`, { method: 'DELETE' });
        return { error: null };
      } catch (error) { return { error }; }
    };
    ```

    **17. apiAddTransactionAndUpdateStock** → POST /api/transactions:
    ```typescript
    export const apiAddTransactionAndUpdateStock = async (
      transaction: Transaction,
      itemId: string,
      itemType: ItemType,
      manualStock: number | null = null
    ): Promise<{ newStock: number; error: any }> => {
      try {
        const data = await apiFetch('/api/transactions', {
          method: 'POST',
          body: JSON.stringify({
            id: transaction.id,
            item_id: itemId,
            item_type: itemType,
            type: transaction.type,
            qty: Number(transaction.qty),
            date: transaction.date,
            notes: transaction.notes || null,
            manual_stock: manualStock ?? null,
          }),
        });
        return { newStock: data.newStock, error: null };
      } catch (error) {
        logError('apiAddTransactionAndUpdateStock', error);
        return { newStock: 0, error };
      }
    };
    ```

    Avoid:
    - Jangan import apapun dari supabase atau supabaseClient
    - Jangan hapus fungsi yang ada — semua harus tetap di-export dengan nama yang sama
    - mapProductFromDB / mapMaterialFromDB / mapTransactionFromDB tidak diperlukan lagi (API sudah return camelCase) — hapus dari file ini
    - mapProductToDB / mapMaterialToDB tidak diperlukan lagi (body JSON langsung di inline function)
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep "services/database" — harus kosong</verify>
  <done>AC-1, AC-2, AC-3, AC-4 satisfied: semua fungsi di-export dengan nama sama, semua pakai fetch() atau authClient</done>
</task>

<task type="auto">
  <name>Task 2: Update App.tsx + cleanup package.json + supabaseClient.ts</name>
  <files>App.tsx, services/supabaseClient.ts, package.json</files>
  <action>
    **2a. Update App.tsx:**

    Hapus baris import supabase:
    ```typescript
    // HAPUS baris ini:
    import { supabase } from './services/supabaseClient';
    ```

    Di dalam `useEffect(() => { ... }, [])` (bagian checkSession), hapus blok `onAuthStateChange`:
    ```typescript
    // HAPUS blok ini:
    const { data: { subscription } } = supabase.auth.onAuthStateChange((_event, session) => {
      if (session) {
        setIsLoggedIn(true);
        setIsPublicMode(false);
        if (!isLoggedIn) loadData();
      } else {
        setIsLoggedIn(false);
        setInventory({ products: [], materials: [], categories: [], transactions: [] });
      }
    });
    return () => subscription.unsubscribe();
    ```

    useEffect yang tersisa harus hanya berisi `checkSession()`:
    ```typescript
    useEffect(() => {
      const checkSession = async () => {
        const session = await getCurrentSession();
        if (session) {
          setIsLoggedIn(true);
          loadData();
        } else {
          setLoading(false);
        }
      };
      checkSession();
    }, []);
    ```

    Tidak ada perubahan lain di App.tsx.

    **2b. Replace services/supabaseClient.ts** dengan stub:
    Replace seluruh isi file dengan:
    ```typescript
    // Migrated to Better Auth. File ini tidak dipakai lagi.
    // Lihat services/database.ts untuk auth functions.
    export {};
    ```

    **2c. Update package.json** — hapus @supabase/supabase-js dari dependencies:
    Hapus baris `"@supabase/supabase-js": "2.39.3",` dari dependencies.
    Simpan file. Jalankan `npm install` untuk update package-lock.json.

    Avoid:
    - Jangan ubah logic lain di App.tsx — hanya hapus 2 bagian supabase
    - Jangan hapus signOut, getCurrentSession imports dari database (masih dipakai)
    - Jangan hapus dependencies lain dari package.json
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -v "supabaseClient.ts" — tidak ada error baru di App.tsx</verify>
  <done>AC-5 satisfied: tidak ada import supabase di App.tsx, onAuthStateChange dihapus, @supabase/supabase-js removed dari package.json</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `api/**` — semua Vercel Functions sudah final
- `lib/**` — sudah final
- `types.ts` — types tidak berubah
- `components/**` — tidak ada perubahan di komponen
- Logic selain supabase di `App.tsx` — jangan ubah handlers lain

## SCOPE LIMITS
- Tidak ada perubahan ke `DashboardView.tsx` atau komponen lain
- Tidak menambah fitur baru — migration only
- `.env.example` sudah punya note DEPRECATED untuk Supabase vars — tidak perlu diubah

</boundaries>

<verification>
Sebelum declare plan complete:
- [ ] `npx tsc --noEmit` — tidak ada error baru (error pre-existing di supabaseClient.ts tidak lagi muncul setelah stub)
- [ ] `services/database.ts` tidak ada import dari supabase atau supabaseClient
- [ ] `App.tsx` tidak ada import dari supabaseClient
- [ ] `App.tsx` tidak ada referensi ke `supabase.auth.onAuthStateChange`
- [ ] `package.json` tidak ada `@supabase/supabase-js` di dependencies
- [ ] Semua 17 fungsi di database.ts masih di-export dengan nama yang sama
- [ ] `authClient.signIn.email()` digunakan untuk signIn (bukan supabase.auth)
- [ ] `credentials: 'include'` ada di apiFetch helper
</verification>

<success_criteria>
- services/database.ts: zero Supabase imports, semua fungsi pakai fetch() atau authClient
- App.tsx: zero supabase references
- package.json: @supabase/supabase-js dihapus
- TypeScript compile bersih untuk file-file yang diubah
- Phase 02 selesai — app siap deploy ke Vercel
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/02-neondb-migration/02-05-SUMMARY.md`
</output>
