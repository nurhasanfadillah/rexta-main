---
phase: 04-drizzle-rebuild
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified: [api/categories.ts, api/products.ts, api/materials.ts, api/products/public.ts, api/products/favorites.ts, api/products/[id]/favorite.ts]
autonomous: true
---

<objective>
## Goal
Rewrite 6 API handlers untuk pakai Drizzle queries yang type-safe — ganti semua `db.query()` (raw Pool, sudah invalid) dan `sql` (neon HTTP adapter, sudah dihapus dari lib/db.ts) dengan Drizzle select/insert/update/delete.

## Purpose
Setelah Plan 04-01, `db` adalah Drizzle instance — bukan Pool lagi. Semua `db.query(...)` dan `sql\`...\`` di api/ handlers sekarang broken (TS error, runtime error). Plan ini fix semua handler kecuali transactions dan dashboard (Plan 04-04).

## Output
- `api/categories.ts` — Drizzle select/insert/delete
- `api/products/favorites.ts` — Drizzle select (favorites)
- `api/products/[id]/favorite.ts` — Drizzle update (toggle)
- `api/products.ts` — Drizzle CRUD + pagination + filtering
- `api/products/public.ts` — Drizzle select + pagination (public, no auth)
- `api/materials.ts` — Drizzle CRUD + pagination + filtering
</objective>

<context>
@.paul/STATE.md
@.paul/phases/04-drizzle-rebuild/04-01-SUMMARY.md
@lib/schema.ts
@lib/db.ts
@api/categories.ts
@api/products.ts
@api/materials.ts
@api/products/public.ts
@api/products/favorites.ts
@api/products/[id]/favorite.ts
</context>

<acceptance_criteria>

## AC-1: Categories API Berfungsi via Drizzle
```gherkin
Given api/categories.ts direwrite dengan Drizzle
When GET /api/categories dipanggil
Then mengembalikan array { id, name } dari tabel categories
And POST dan DELETE berjalan tanpa raw sql atau db.query()
And tidak ada import sql atau db.query di file
```

## AC-2: Products API Berfungsi via Drizzle (Semua Endpoint)
```gherkin
Given api/products.ts + sub-routes direwrite dengan Drizzle
When GET /api/products?page=1&limit=20&search=x&categoryId=y dipanggil
Then mengembalikan { data: ProductRow[], count: number } dengan pagination benar
And POST/PUT/DELETE berjalan, PUT mengembalikan 404 jika id tidak ditemukan
And /products/favorites mengembalikan list produk is_favorite=true
And /products/[id]/favorite PATCH toggle isFavorite dan return 404 jika tidak ada
And /products/public mengembalikan data + categories tanpa auth
```

## AC-3: Materials API Berfungsi via Drizzle
```gherkin
Given api/materials.ts direwrite dengan Drizzle
When GET /api/materials?page=1&limit=20&search=x dipanggil
Then mengembalikan { data: MaterialRow[], count: number } dengan pagination benar
And POST/PUT/DELETE berjalan, PUT mengembalikan 404 jika id tidak ditemukan
And tidak ada raw db.query() atau sql template
```

## AC-4: TypeScript Bersih di Semua File yang Dimodifikasi
```gherkin
Given npm install sudah dijalankan (drizzle-orm tersedia)
When npx tsc --noEmit dijalankan
Then tidak ada error baru di semua 6 file yang dimodifikasi plan ini
And error yang tersisa hanya di api/transactions.ts dan api/dashboard.ts (Plan 04-04)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite api/categories.ts + api/products/favorites.ts + api/products/[id]/favorite.ts</name>
  <files>api/categories.ts, api/products/favorites.ts, api/products/[id]/favorite.ts</files>
  <action>
    Tiga file kecil dengan operasi sederhana — tidak ada pagination, tidak ada filtering kompleks.

    **Drizzle operators yang dibutuhkan:** `eq`, `asc` dari `drizzle-orm`.
    **Schema imports:** `categories`, `products` dari `../lib/schema.js` (atau path relative masing-masing).

    ---

    **Ganti SELURUH isi `api/categories.ts` dengan:**
    ```typescript
    import { db } from '../lib/db.js';
    import { categories } from '../lib/schema.js';
    import { eq, asc } from 'drizzle-orm';
    import { requireSession } from '../lib/auth-middleware.js';

    export default async function handler(req: any, res: any) {
      if (req.method === 'GET') {
        try {
          const rows = await db.select({ id: categories.id, name: categories.name })
            .from(categories).orderBy(asc(categories.name));
          return res.json(rows);
        } catch (error) {
          console.error('[categories] GET error:', error);
          return res.status(500).json({ error: 'Internal Server Error' });
        }
      }

      try {
        await requireSession(req);
      } catch (e: any) {
        return res.status(401).json({ error: 'Unauthorized' });
      }

      if (req.method === 'POST') {
        try {
          const { id, name } = req.body;
          const [row] = await db.insert(categories).values({ id, name }).returning();
          return res.json(row);
        } catch (error) {
          console.error('[categories] POST error:', error);
          return res.status(500).json({ error: 'Internal Server Error' });
        }
      }

      if (req.method === 'DELETE') {
        try {
          const { id } = req.query;
          await db.delete(categories).where(eq(categories.id, id as string));
          return res.json({ success: true });
        } catch (error) {
          console.error('[categories] DELETE error:', error);
          return res.status(500).json({ error: 'Internal Server Error' });
        }
      }

      return res.status(405).json({ error: 'Method not allowed' });
    }
    ```

    ---

    **Ganti SELURUH isi `api/products/favorites.ts` dengan:**
    ```typescript
    import { db } from '../../lib/db.js';
    import { products } from '../../lib/schema.js';
    import { eq, asc } from 'drizzle-orm';
    import { requireSession } from '../../lib/auth-middleware.js';

    const mapProduct = (row: typeof products.$inferSelect) => ({
      id: row.id,
      name: row.name,
      categoryId: row.categoryId,
      priceCMT: Number(row.priceCmt) || 0,
      hpp: Number(row.hpp) || 0,
      stock: Number(row.stock) || 0,
      updatedAt: row.updatedAt,
      isFavorite: row.isFavorite ?? false,
    });

    export default async function handler(req: any, res: any) {
      if (req.method !== 'GET') {
        return res.status(405).json({ error: 'Method not allowed' });
      }

      try {
        await requireSession(req);
      } catch (e: any) {
        return res.status(401).json({ error: 'Unauthorized' });
      }

      try {
        const rows = await db.select().from(products)
          .where(eq(products.isFavorite, true)).orderBy(asc(products.name));
        return res.json(rows.map(mapProduct));
      } catch (error) {
        console.error('[products/favorites] GET error:', error);
        return res.status(500).json({ error: 'Internal Server Error' });
      }
    }
    ```

    ---

    **Ganti SELURUH isi `api/products/[id]/favorite.ts` dengan:**
    ```typescript
    import { db } from '../../../lib/db.js';
    import { products } from '../../../lib/schema.js';
    import { eq } from 'drizzle-orm';
    import { requireSession } from '../../../lib/auth-middleware.js';

    export default async function handler(req: any, res: any) {
      if (req.method !== 'PATCH') {
        return res.status(405).json({ error: 'Method not allowed' });
      }

      try {
        await requireSession(req);
      } catch (e: any) {
        return res.status(401).json({ error: 'Unauthorized' });
      }

      try {
        const { id } = req.query;
        const { isFavorite } = req.body;
        const result = await db.update(products)
          .set({ isFavorite })
          .where(eq(products.id, id as string))
          .returning({ id: products.id });
        if (result.length === 0) return res.status(404).json({ error: 'Product not found' });
        return res.json({ success: true });
      } catch (error) {
        console.error('[products/[id]/favorite] PATCH error:', error);
        return res.status(500).json({ error: 'Internal Server Error' });
      }
    }
    ```

    **Avoid:**
    - Jangan import `sql` atau `Pool` di file-file ini
    - Jangan gunakan `db.query()` — db sekarang Drizzle instance, bukan Pool
    - `id as string` diperlukan karena `req.query.id` bertipe `string | string[]` di TypeScript
  </action>
  <verify>
    - `grep "sql\`" api/categories.ts` → tidak ada
    - `grep "db\.query" api/categories.ts api/products/favorites.ts "api/products/[id]/favorite.ts"` → tidak ada
    - `grep "drizzle-orm" api/categories.ts` → ada (import eq, asc)
    - `grep "from.*schema" api/products/favorites.ts` → ada (import products)
  </verify>
  <done>AC-1 terpenuhi (categories). Foundation AC-2 (favorites + favorite-toggle) terpenuhi.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite api/products.ts + api/products/public.ts</name>
  <files>api/products.ts, api/products/public.ts</files>
  <action>
    Dua file dengan pagination dan filtering. Keduanya products-related.

    **Drizzle operators:** `eq`, `ilike`, `and`, `asc`, `count` dari `drizzle-orm`.
    **Key pattern pagination:**
    ```typescript
    const [[{ value: total }], data] = await Promise.all([
      db.select({ value: count() }).from(products).where(where),
      db.select().from(products).where(where).orderBy(asc(products.name)).limit(limit).offset(offset),
    ]);
    ```
    `count()` mengembalikan number. `where` bisa `undefined` (tanpa filter) atau result dari `and(...)`.

    **Mapper update:** Drizzle mengembalikan field names TypeScript (camelCase dari schema), bukan kolom DB. Ganti `row.category_id` → `row.categoryId`, `row.price_cmt` → `row.priceCmt`, dll.

    ---

    **Ganti SELURUH isi `api/products.ts` dengan:**
    ```typescript
    import { db } from '../lib/db.js';
    import { products } from '../lib/schema.js';
    import { eq, ilike, and, asc, count } from 'drizzle-orm';
    import { requireSession } from '../lib/auth-middleware.js';

    const mapProduct = (row: typeof products.$inferSelect) => ({
      id: row.id,
      name: row.name,
      categoryId: row.categoryId,
      priceCMT: Number(row.priceCmt) || 0,
      hpp: Number(row.hpp) || 0,
      stock: Number(row.stock) || 0,
      updatedAt: row.updatedAt,
      isFavorite: row.isFavorite ?? false,
    });

    export default async function handler(req: any, res: any) {
      try {
        await requireSession(req);
      } catch (e: any) {
        return res.status(401).json({ error: 'Unauthorized' });
      }

      if (req.method === 'GET') {
        try {
          const page = Number(req.query.page) || 1;
          const limit = Number(req.query.limit) || 20;
          const search = req.query.search || '';
          const categoryId = req.query.categoryId || '';
          const onlyFavorites = req.query.onlyFavorites === 'true';
          const offset = (page - 1) * limit;

          const conditions = [];
          if (search) conditions.push(ilike(products.name, `%${search}%`));
          if (categoryId && categoryId !== 'SEMUA') conditions.push(eq(products.categoryId, categoryId as string));
          if (onlyFavorites) conditions.push(eq(products.isFavorite, true));
          const where = conditions.length > 0 ? and(...conditions) : undefined;

          const [[{ value: total }], data] = await Promise.all([
            db.select({ value: count() }).from(products).where(where),
            db.select().from(products).where(where).orderBy(asc(products.name)).limit(limit).offset(offset),
          ]);

          return res.json({ data: data.map(mapProduct), count: Number(total) });
        } catch (error) {
          console.error('[products] GET error:', error);
          return res.status(500).json({ error: 'Internal Server Error' });
        }
      }

      if (req.method === 'POST') {
        try {
          const { id, name, category_id, price_cmt, hpp, stock, is_favorite } = req.body;
          const [row] = await db.insert(products).values({
            id,
            name,
            categoryId: category_id || null,
            priceCmt: String(price_cmt || 0),
            hpp: String(hpp || 0),
            stock: String(stock || 0),
            isFavorite: is_favorite || false,
          }).returning();
          return res.json(mapProduct(row));
        } catch (error) {
          console.error('[products] POST error:', error);
          return res.status(500).json({ error: 'Internal Server Error' });
        }
      }

      if (req.method === 'PUT') {
        try {
          const { id } = req.query;
          const { name, category_id, price_cmt, hpp, stock, is_favorite } = req.body;
          const [row] = await db.update(products).set({
            name,
            categoryId: category_id || null,
            priceCmt: String(price_cmt || 0),
            hpp: String(hpp || 0),
            stock: String(stock || 0),
            isFavorite: is_favorite || false,
            updatedAt: new Date(),
          }).where(eq(products.id, id as string)).returning();
          if (!row) return res.status(404).json({ error: 'Product not found' });
          return res.json(mapProduct(row));
        } catch (error) {
          console.error('[products] PUT error:', error);
          return res.status(500).json({ error: 'Internal Server Error' });
        }
      }

      if (req.method === 'DELETE') {
        try {
          const { id } = req.query;
          await db.delete(products).where(eq(products.id, id as string));
          return res.json({ success: true });
        } catch (error) {
          console.error('[products] DELETE error:', error);
          return res.status(500).json({ error: 'Internal Server Error' });
        }
      }

      return res.status(405).json({ error: 'Method not allowed' });
    }
    ```

    ---

    **Ganti SELURUH isi `api/products/public.ts` dengan:**
    ```typescript
    import { db } from '../../lib/db.js';
    import { products, categories } from '../../lib/schema.js';
    import { ilike, asc, count } from 'drizzle-orm';

    export default async function handler(req: any, res: any) {
      if (req.method !== 'GET') {
        return res.status(405).json({ error: 'Method not allowed' });
      }

      try {
        const page = Number(req.query.page) || 1;
        const limit = Number(req.query.limit) || 20;
        const search = req.query.search || '';
        const offset = (page - 1) * limit;

        const where = search ? ilike(products.name, `%${search}%`) : undefined;

        const [[{ value: total }], data, cats] = await Promise.all([
          db.select({ value: count() }).from(products).where(where),
          db.select({ id: products.id, name: products.name, stock: products.stock, categoryId: products.categoryId })
            .from(products).where(where).orderBy(asc(products.name)).limit(limit).offset(offset),
          db.select({ id: categories.id, name: categories.name }).from(categories).orderBy(asc(categories.name)),
        ]);

        return res.json({
          data: data.map(row => ({
            id: row.id,
            name: row.name,
            stock: Number(row.stock) || 0,
            categoryId: row.categoryId,
          })),
          categories: cats,
          count: Number(total),
        });
      } catch (error) {
        console.error('[products/public] GET error:', error);
        return res.status(500).json({ error: 'Internal Server Error' });
      }
    }
    ```

    **Kenapa ini benar:**
    - Body fields POST/PUT tetap snake_case (`category_id`, `price_cmt`, `is_favorite`) — sesuai keputusan di STATE.md
    - Drizzle field names (TypeScript) di schema adalah camelCase: `categoryId`, `priceCmt`, `isFavorite`
    - Numeric fields (`priceCmt`, `hpp`, `stock`, `price`) diinsert sebagai `String()` karena Drizzle `numeric` type
    - `updatedAt: new Date()` di PUT — Drizzle tidak auto-update timestamp
    - `conditions.length > 0 ? and(...conditions) : undefined` — `and()` dengan 0 args akan error

    **Avoid:**
    - Jangan gunakan `db.query()` — ini Drizzle instance, bukan Pool
    - Jangan import `sql` dari lib/db.ts — sudah dihapus
    - Jangan spread `and(...conditions)` jika conditions kosong — gunakan `undefined` langsung
    - Jangan lupa `updatedAt: new Date()` di PUT — tanpa ini field tidak terupdate
  </action>
  <verify>
    - `grep "db\.query" api/products.ts` → tidak ada
    - `grep "sql\`" api/products/public.ts` → tidak ada
    - `grep "count()" api/products.ts api/products/public.ts` → ada (pagination)
    - `grep "ilike" api/products.ts` → ada (search filter)
    - `grep "updatedAt.*new Date" api/products.ts` → ada di PUT handler
  </verify>
  <done>AC-2 terpenuhi: semua product endpoints (main + public + favorites + toggle) pakai Drizzle.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite api/materials.ts</name>
  <files>api/materials.ts</files>
  <action>
    Pola identik dengan products.ts — CRUD + pagination + search filtering. Satu file, satu concern.

    **Ganti SELURUH isi `api/materials.ts` dengan:**
    ```typescript
    import { db } from '../lib/db.js';
    import { materials } from '../lib/schema.js';
    import { eq, ilike, asc, count } from 'drizzle-orm';
    import { requireSession } from '../lib/auth-middleware.js';

    const mapMaterial = (row: typeof materials.$inferSelect) => ({
      id: row.id,
      name: row.name,
      unit: row.unit,
      price: Number(row.price) || 0,
      stock: Number(row.stock) || 0,
      updatedAt: row.updatedAt,
    });

    export default async function handler(req: any, res: any) {
      try {
        await requireSession(req);
      } catch (e: any) {
        return res.status(401).json({ error: 'Unauthorized' });
      }

      if (req.method === 'GET') {
        try {
          const page = Number(req.query.page) || 1;
          const limit = Number(req.query.limit) || 20;
          const search = req.query.search || '';
          const offset = (page - 1) * limit;

          const where = search ? ilike(materials.name, `%${search}%`) : undefined;

          const [[{ value: total }], data] = await Promise.all([
            db.select({ value: count() }).from(materials).where(where),
            db.select().from(materials).where(where).orderBy(asc(materials.name)).limit(limit).offset(offset),
          ]);

          return res.json({ data: data.map(mapMaterial), count: Number(total) });
        } catch (error) {
          console.error('[materials] GET error:', error);
          return res.status(500).json({ error: 'Internal Server Error' });
        }
      }

      if (req.method === 'POST') {
        try {
          const { id, name, unit, price, stock } = req.body;
          const [row] = await db.insert(materials).values({
            id,
            name,
            unit,
            price: String(price || 0),
            stock: String(stock || 0),
          }).returning();
          return res.json(mapMaterial(row));
        } catch (error) {
          console.error('[materials] POST error:', error);
          return res.status(500).json({ error: 'Internal Server Error' });
        }
      }

      if (req.method === 'PUT') {
        try {
          const { id } = req.query;
          const { name, unit, price, stock } = req.body;
          const [row] = await db.update(materials).set({
            name,
            unit,
            price: String(price || 0),
            stock: String(stock || 0),
            updatedAt: new Date(),
          }).where(eq(materials.id, id as string)).returning();
          if (!row) return res.status(404).json({ error: 'Material not found' });
          return res.json(mapMaterial(row));
        } catch (error) {
          console.error('[materials] PUT error:', error);
          return res.status(500).json({ error: 'Internal Server Error' });
        }
      }

      if (req.method === 'DELETE') {
        try {
          const { id } = req.query;
          await db.delete(materials).where(eq(materials.id, id as string));
          return res.json({ success: true });
        } catch (error) {
          console.error('[materials] DELETE error:', error);
          return res.status(500).json({ error: 'Internal Server Error' });
        }
      }

      return res.status(405).json({ error: 'Method not allowed' });
    }
    ```

    **Avoid:**
    - Jangan gunakan `db.query()` — sudah bukan Pool
    - `updatedAt: new Date()` di PUT wajib ada
    - `id as string` di where clause untuk TypeScript
  </action>
  <verify>
    - `grep "db\.query" api/materials.ts` → tidak ada
    - `grep "count()" api/materials.ts` → ada
    - `grep "ilike" api/materials.ts` → ada
    - `grep "updatedAt.*new Date" api/materials.ts` → ada di PUT
  </verify>
  <done>AC-3 terpenuhi: materials CRUD + pagination via Drizzle.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `api/transactions.ts` — akan direwrite di Plan 04-04 (atomic transaction logic)
- `api/dashboard.ts` — akan direwrite di Plan 04-04 (aggregate queries)
- `api/health.ts` — tidak pakai db, tidak perlu berubah
- `api/auth-proxy.ts` — sudah benar dari Plan 04-02
- `lib/auth.ts`, `lib/db.ts`, `lib/schema.ts`, `lib/auth-middleware.ts` — foundation plans sebelumnya
- Semua React components, types.ts, services/database.ts, vercel.json

## SCOPE LIMITS
- Plan ini hanya menyentuh 6 file API di atas
- Tidak ada perubahan ke response shape — frontend harus tetap bekerja tanpa perubahan
- Tidak ada perubahan ke auth guard logic (requireSession pattern tetap sama)
- Tidak ada feature baru — hanya rewrite transport layer (raw SQL → Drizzle)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -r "db\.query" api/categories.ts api/products.ts api/materials.ts api/products/public.ts api/products/favorites.ts` → 0 hasil
- [ ] `grep -r "sql\`" api/categories.ts api/products.ts api/materials.ts api/products/public.ts api/products/favorites.ts` → 0 hasil
- [ ] `grep -r "from 'drizzle-orm'" api/categories.ts api/products.ts api/materials.ts` → ada di semua 3
- [ ] `grep "count()" api/products.ts api/products/public.ts api/materials.ts` → ada di semua 3
- [ ] `grep "updatedAt.*new Date" api/products.ts api/materials.ts` → ada di keduanya
- [ ] `npx tsc --noEmit` → error HANYA di api/transactions.ts dan api/dashboard.ts — tidak ada error baru di 6 file yang dimodifikasi
</verification>

<success_criteria>
- 6 API handlers bebas dari raw db.query() dan sql template
- Semua response shapes tidak berubah (frontend kompatibel)
- TypeScript bersih di semua 6 file setelah npm install
- api/transactions.ts dan api/dashboard.ts masih error (expected, Plan 04-04)
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/04-drizzle-rebuild/04-03-SUMMARY.md`
</output>
