---
phase: 04-drizzle-rebuild
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified: [api/transactions.ts, api/dashboard.ts]
autonomous: true
---

<objective>
## Goal
Rewrite 2 API handlers terakhir — `api/transactions.ts` dan `api/dashboard.ts` — dari raw `db.query()` ke Drizzle ORM, mengganti stored procedure `process_inventory_transaction()` dengan `db.transaction()` Drizzle yang atomic.

## Purpose
Setelah Plan 04-03, semua 6 handler sederhana sudah Drizzle. Dua file ini tersisa karena lebih kompleks:
- `api/transactions.ts`: butuh atomic transaction (bukan query sederhana) + date range filtering
- `api/dashboard.ts`: butuh aggregate queries (COUNT, SUM dengan aritmatik antar kolom)

## Output
- `api/transactions.ts` — Drizzle GET (paginated + filtered) + POST (db.transaction() atomic)
- `api/dashboard.ts` — Drizzle GET (aggregate stats + recent transactions)
</objective>

<context>
@.paul/STATE.md
@.paul/phases/04-drizzle-rebuild/04-03-SUMMARY.md
@lib/schema.ts
@lib/db.ts
@api/transactions.ts
@api/dashboard.ts
</context>

<acceptance_criteria>

## AC-1: Transactions GET Berfungsi via Drizzle
```gherkin
Given api/transactions.ts direwrite dengan Drizzle
When GET /api/transactions?itemId=x&itemType=product&dateFrom=2024-01-01&dateTo=2024-12-31&page=1&limit=20 dipanggil
Then mengembalikan { data: TransactionRow[], count: number } dengan pagination benar
And filter itemId, itemType, dateFrom, dateTo berfungsi secara dynamic (kombinasi apapun)
And tidak ada db.query() atau raw SQL string di handler
```

## AC-2: Transactions POST Atomic via db.transaction()
```gherkin
Given api/transactions.ts POST menggunakan db.transaction()
When POST /api/transactions dipanggil dengan { id, item_id, item_type, type, qty, date, notes, manual_stock }
Then stok item diupdate dan record transaksi dibuat secara atomic (keduanya berhasil atau keduanya rollback)
And type=IN menambah stok, type=OUT mengurangi stok, type=OPNAME mengoverride ke manual_stock
And balanceAfter di record transaksi sama dengan stok baru setelah operasi
And return { transaction: TransactionRow, newStock: number }
And jika item tidak ditemukan, transaksi di-rollback dan return 404
```

## AC-3: Dashboard GET Berfungsi via Drizzle
```gherkin
Given api/dashboard.ts direwrite dengan Drizzle
When GET /api/dashboard dipanggil
Then mengembalikan { totalProducts, totalMaterials, productAssetValue, materialAssetValue, totalAssetValue, recentTransactions }
And totalProducts dan totalMaterials adalah count integer
And productAssetValue = SUM(stock * priceCmt) dan materialAssetValue = SUM(stock * price)
And recentTransactions adalah 10 transaksi terbaru urut date DESC
And tidak ada db.query() atau raw SQL string
```

## AC-4: TypeScript Bersih di Semua File yang Dimodifikasi
```gherkin
Given npm install sudah dijalankan (drizzle-orm tersedia)
When npx tsc --noEmit dijalankan
Then tidak ada error baru di api/transactions.ts dan api/dashboard.ts
And Phase 04 selesai: zero db.query() di seluruh api/ (kecuali api/health.ts yang tidak pakai db)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite api/transactions.ts — GET (paginated + filtered) + POST (atomic transaction)</name>
  <files>api/transactions.ts</files>
  <action>
    Dua endpoint dalam satu file: GET dengan dynamic filters + pagination, POST dengan atomic transaction menggantikan stored procedure.

    **Schema transactions fields (camelCase Drizzle, bukan snake_case DB):**
    - `transactions.itemId` (kolom: item_id)
    - `transactions.itemType` (kolom: item_type)
    - `transactions.type`
    - `transactions.qty` (numeric)
    - `transactions.date` (timestamp)
    - `transactions.notes`
    - `transactions.balanceAfter` (kolom: balance_after, numeric)

    **Drizzle operators yang dibutuhkan:** `eq`, `gte`, `lte`, `and`, `desc`, `count` dari `drizzle-orm`.

    ---

    **Ganti SELURUH isi `api/transactions.ts` dengan:**
    ```typescript
    import { db } from '../lib/db.js';
    import { transactions, products, materials } from '../lib/schema.js';
    import { eq, gte, lte, and, desc, count } from 'drizzle-orm';
    import { requireSession } from '../lib/auth-middleware.js';

    const mapTransaction = (row: typeof transactions.$inferSelect) => ({
      id: row.id,
      itemId: row.itemId,
      itemType: row.itemType,
      type: row.type,
      qty: Number(row.qty),
      date: row.date,
      notes: row.notes || null,
      balanceAfter: Number(row.balanceAfter),
    });

    export default async function handler(req: any, res: any) {
      try {
        await requireSession(req);
      } catch (e: any) {
        return res.status(401).json({ error: 'Unauthorized' });
      }

      if (req.method === 'GET') {
        try {
          const page = Number(req.query.page) || 1;
          const limit = Number(req.query.limit) || 20;
          const { itemId, itemType, dateFrom, dateTo } = req.query;
          const offset = (page - 1) * limit;

          const conditions = [];
          if (itemId) conditions.push(eq(transactions.itemId, itemId as string));
          if (itemType) conditions.push(eq(transactions.itemType, itemType as string));
          if (dateFrom) conditions.push(gte(transactions.date, new Date(dateFrom as string)));
          if (dateTo) conditions.push(lte(transactions.date, new Date(dateTo as string)));
          const where = conditions.length > 0 ? and(...conditions) : undefined;

          const [[{ value: total }], data] = await Promise.all([
            db.select({ value: count() }).from(transactions).where(where),
            db.select().from(transactions).where(where).orderBy(desc(transactions.date)).limit(limit).offset(offset),
          ]);

          return res.json({ data: data.map(mapTransaction), count: Number(total) });
        } catch (error) {
          console.error('[transactions] GET error:', error);
          return res.status(500).json({ error: 'Internal Server Error' });
        }
      }

      if (req.method === 'POST') {
        try {
          const { id, item_id, item_type, type, qty, date, notes, manual_stock } = req.body;

          const result = await db.transaction(async (tx) => {
            let currentStock: string;

            if (item_type === 'product') {
              const [item] = await tx.select({ stock: products.stock })
                .from(products).where(eq(products.id, item_id));
              if (!item) throw new Error('ITEM_NOT_FOUND');
              currentStock = item.stock || '0';
            } else {
              const [item] = await tx.select({ stock: materials.stock })
                .from(materials).where(eq(materials.id, item_id));
              if (!item) throw new Error('ITEM_NOT_FOUND');
              currentStock = item.stock || '0';
            }

            const current = Number(currentStock);
            const newStock =
              type === 'OPNAME' ? Number(manual_stock) :
              type === 'IN'     ? current + Number(qty) :
                                  current - Number(qty);

            if (item_type === 'product') {
              await tx.update(products)
                .set({ stock: String(newStock), updatedAt: new Date() })
                .where(eq(products.id, item_id));
            } else {
              await tx.update(materials)
                .set({ stock: String(newStock), updatedAt: new Date() })
                .where(eq(materials.id, item_id));
            }

            const [txRecord] = await tx.insert(transactions).values({
              id,
              itemId: item_id,
              itemType: item_type,
              type,
              qty: String(qty),
              date: new Date(date),
              notes: notes || null,
              balanceAfter: String(newStock),
            }).returning();

            return { transaction: txRecord, newStock };
          });

          return res.json({
            transaction: mapTransaction(result.transaction),
            newStock: result.newStock,
          });
        } catch (error: any) {
          if (error?.message === 'ITEM_NOT_FOUND') {
            return res.status(404).json({ error: 'Item not found' });
          }
          console.error('[transactions] POST error:', error);
          return res.status(500).json({ error: 'Internal Server Error' });
        }
      }

      return res.status(405).json({ error: 'Method not allowed' });
    }
    ```

    **Key decisions:**
    - `db.transaction(async (tx) => { ... })` — Drizzle atomic transaction, rollback otomatis jika throw
    - Error `ITEM_NOT_FOUND` di-throw dari dalam transaction untuk trigger rollback + 404 response di outer catch
    - `date: new Date(date)` — konversi string date dari body ke Date object untuk timestamp column
    - `updatedAt: new Date()` di update stock — konsisten dengan pola di Plan 04-03
    - `item_type === 'product'` vs `'material'` — branch logic untuk product/material table berbeda

    **Avoid:**
    - Jangan call `db.query()` — db adalah Drizzle instance, bukan Pool
    - Jangan import stored procedure RPC — ganti sepenuhnya dengan `db.transaction()`
    - Jangan throw generic Error di dalam transaction untuk item not found — pakai sentinel string `'ITEM_NOT_FOUND'` agar bisa dibedakan dari DB errors di outer catch
  </action>
  <verify>
    - `grep "db\.query" api/transactions.ts` → tidak ada
    - `grep "process_inventory_transaction" api/transactions.ts` → tidak ada (RPC dihapus)
    - `grep "db\.transaction" api/transactions.ts` → ada (atomic transaction)
    - `grep "ITEM_NOT_FOUND" api/transactions.ts` → ada (sentinel error untuk 404)
    - `grep "drizzle-orm" api/transactions.ts` → ada (eq, gte, lte, and, desc, count)
  </verify>
  <done>AC-1 dan AC-2 terpenuhi: GET transactions dengan dynamic filter + pagination, POST via db.transaction() atomic menggantikan RPC.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite api/dashboard.ts — aggregate stats + recent transactions</name>
  <files>api/dashboard.ts</files>
  <action>
    Satu GET endpoint dengan aggregate queries: COUNT products dan materials, SUM asset values (stock * price), dan 10 recent transactions.

    **Approach:** Gunakan `sql` dari `drizzle-orm` untuk aritmatik antar kolom dalam aggregate (SUM(stock * price)). Ini BERBEDA dari `sql` yang dihapus di Plan 04-01 — `sql` dari `drizzle-orm` adalah tagged template untuk SQL fragments di dalam query builder, bukan untuk menjalankan full raw query.

    **Drizzle operators:** `count`, `desc`, `sql` dari `drizzle-orm`.

    ---

    **Ganti SELURUH isi `api/dashboard.ts` dengan:**
    ```typescript
    import { db } from '../lib/db.js';
    import { products, materials, transactions } from '../lib/schema.js';
    import { count, desc, sql } from 'drizzle-orm';
    import { requireSession } from '../lib/auth-middleware.js';

    const mapTransaction = (row: typeof transactions.$inferSelect) => ({
      id: row.id,
      itemId: row.itemId,
      itemType: row.itemType,
      type: row.type,
      qty: Number(row.qty),
      date: row.date,
      notes: row.notes || null,
      balanceAfter: Number(row.balanceAfter),
    });

    export default async function handler(req: any, res: any) {
      try {
        await requireSession(req);
      } catch (e: any) {
        return res.status(401).json({ error: 'Unauthorized' });
      }

      if (req.method !== 'GET') {
        return res.status(405).json({ error: 'Method not allowed' });
      }

      try {
        const [
          [{ value: totalProducts }],
          [{ value: totalMaterials }],
          [{ value: productAssetRaw }],
          [{ value: materialAssetRaw }],
          recentTxs,
        ] = await Promise.all([
          db.select({ value: count() }).from(products),
          db.select({ value: count() }).from(materials),
          db.select({
            value: sql<string>`COALESCE(SUM(${products.stock}::numeric * ${products.priceCmt}::numeric), 0)`,
          }).from(products),
          db.select({
            value: sql<string>`COALESCE(SUM(${materials.stock}::numeric * ${materials.price}::numeric), 0)`,
          }).from(materials),
          db.select().from(transactions).orderBy(desc(transactions.date)).limit(10),
        ]);

        const productAssetValue = Number(productAssetRaw);
        const materialAssetValue = Number(materialAssetRaw);

        return res.json({
          totalProducts: Number(totalProducts),
          totalMaterials: Number(totalMaterials),
          productAssetValue,
          materialAssetValue,
          totalAssetValue: productAssetValue + materialAssetValue,
          recentTransactions: recentTxs.map(mapTransaction),
        });
      } catch (error) {
        console.error('[dashboard] GET error:', error);
        return res.status(500).json({ error: 'Internal Server Error' });
      }
    }
    ```

    **Kenapa `sql` dari drizzle-orm dipakai di sini:**
    - `SUM(stock * price_cmt)` tidak bisa diekspresikan dengan Drizzle API murni tanpa `sql` template
    - `sql` dari `drizzle-orm` ≠ `sql` yang dihapus (yang lama dari neon HTTP adapter / lib/db.ts)
    - Column references `${products.stock}` dalam `sql` template di-resolve Drizzle ke nama kolom yang benar
    - `::numeric` cast diperlukan karena kolom `numeric` di Drizzle bisa berupa string saat dibaca

    **Avoid:**
    - Jangan import `sql` dari `lib/db.ts` (sudah dihapus dan tidak ada di sana)
    - Jangan gunakan `db.query()` — Drizzle instance, bukan Pool
    - Jangan lupa `COALESCE(..., 0)` — jika tabel kosong, SUM() mengembalikan NULL bukan 0
  </action>
  <verify>
    - `grep "db\.query" api/dashboard.ts` → tidak ada
    - `grep "drizzle-orm" api/dashboard.ts` → ada (count, desc, sql)
    - `grep "COALESCE" api/dashboard.ts` → ada (asset value null guard)
    - `grep "from 'drizzle-orm'" api/dashboard.ts` → ada
  </verify>
  <done>AC-3 terpenuhi: dashboard stats + recent transactions via Drizzle aggregate queries.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `lib/db.ts`, `lib/schema.ts`, `lib/auth.ts`, `lib/auth-middleware.ts` — foundation dari Plan 04-01 dan 04-02
- `api/categories.ts`, `api/products.ts`, `api/materials.ts`, `api/products/public.ts`, `api/products/favorites.ts`, `api/products/[id]/favorite.ts` — sudah Drizzle dari Plan 04-03
- `api/auth-proxy.ts` — sudah benar dari Plan 04-02
- `api/health.ts` — tidak pakai db, tidak perlu berubah
- Semua React components, `types.ts`, `services/database.ts`, `vercel.json`
- `drizzle.config.ts`, `scripts/seed-user.mjs` — infrastructure scripts, bukan scope plan ini

## SCOPE LIMITS
- Plan ini hanya menyentuh 2 file: `api/transactions.ts` dan `api/dashboard.ts`
- Tidak ada perubahan ke response shape — frontend harus tetap bekerja
- Tidak ada perubahan ke auth guard logic (requireSession pattern tetap sama)
- Tidak ada feature baru — hanya rewrite transport layer (raw SQL + RPC → Drizzle)
- `sql` dari `drizzle-orm` diizinkan untuk aggregate arithmetic — ini berbeda dari `sql` yang dihapus

</boundaries>

<verification>
Sebelum declare plan complete:
- [ ] `grep -r "db\.query" api/transactions.ts api/dashboard.ts` → 0 hasil
- [ ] `grep "process_inventory_transaction" api/transactions.ts` → 0 hasil (RPC dihapus)
- [ ] `grep "db\.transaction" api/transactions.ts` → ada (atomic POST)
- [ ] `grep "from 'drizzle-orm'" api/transactions.ts api/dashboard.ts` → ada di keduanya
- [ ] `grep "COALESCE" api/dashboard.ts` → ada di kedua asset value queries
- [ ] `grep -r "db\.query" api/` → 0 hasil di seluruh api/ (Phase 04 selesai)
- [ ] `npx tsc --noEmit` → tidak ada error baru di 2 file yang dimodifikasi
</verification>

<success_criteria>
- api/transactions.ts: GET dengan 4 filter dynamic + pagination via Drizzle, POST dengan db.transaction() atomic
- api/dashboard.ts: aggregate queries COUNT + SUM via Drizzle + sql template
- Zero raw `db.query()` di seluruh direktori `api/` — Phase 04 goal tercapai
- Response shapes tidak berubah — frontend tetap kompatibel
- TypeScript bersih di kedua file
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/04-drizzle-rebuild/04-04-SUMMARY.md`
</output>
